package com.mex.GalaxyChain.mychart.chart.linechat.stockchart;

/**
 * author：LSJ
 */
public class Test {

   /* private ArrayList<Integer> listA, listB;
    private  int sum;

    public static void main(String[] args) {
        Test test=new Test();
        test.listA = new ArrayList<>();
        test.listB = new ArrayList<>();
        for (int i = 0; i < 10; i++) {
            test.listA.add(i, i);
        }

        for (int i = 0; i < 10; i++) {

            if (i >= 4) {
                test.sum = 0;
                test.listB.add(i, test.fund(i - 4, i));
            } else {
                test.listB.add(i, 0);
            }
        }

        for (int i = 0; i < 10; i++) {
            System.out.print(test.listB.get(i) + "   ");
        }

    }

    public  Integer fund(Integer StatementEntity, Integer b) {

        for (int i = StatementEntity; i <= b; i++) {
            sum += listA.get(i);
        }
        return sum;
    }
*/
       /*public boolean foo(char c) {
           System.out.print(c);
           return true;
       }
       public static void main(String[] argv) {
           Test test=new Test();

           int i = 0;
           for (test.foo('A'); test.foo('B') && (i < 2); test.foo('C')) {
               i++;
               test.foo('D');
           }
       }*/

    /*public static void main(String[] argv) {
        int[] StatementEntity={2,3,1,6,4};
        for(int i=0;i<StatementEntity.length-1;i++){
            for(int j=0;j<StatementEntity.length-i-1;j++){
                if(StatementEntity[j]>StatementEntity[j+1]){
                    int temp=StatementEntity[j+1];
                    StatementEntity[j+1]=StatementEntity[j];
                    StatementEntity[j]=temp;
                }
            }
        }

        for (int i:StatementEntity) {
            System.out.print(i+",");

        }
    }*/
}
