<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 实心 -->
    <solid android:color="@color/btn_bg_blue_pressed" />
    <!-- 边框 -->
    <stroke
        android:width="@dimen/dp1"
        android:color="@color/btn_bg_blue_pressed" />
    <!-- 圆角 -->
    <corners android:radius="@dimen/dp20" />
    <!-- 边距 -->
    <padding
        android:left="@dimen/dp10"
        android:right="@dimen/dp10" />
    <!-- 渐变 -->
    <!--    <gradient
            android:angle="270"
            android:endColor="#FFFF782"
            android:startColor="#13C7AF" />-->
</shape>